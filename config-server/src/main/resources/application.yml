server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/vikingyang/SpringCloudLearn.git
          search-paths: config

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/